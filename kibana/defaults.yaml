# -*- coding: utf-8 -*-
# vim: ft=yaml
kibana:
  pkg: kibana # This is not used
  config: '/etc/kibana' # This is not used
  source: true # Install from source
  sourceVersion: "4.1.6"
  sources:
    installPath: '/opt/kibana/'
    baseURL: "https://download.elastic.co/kibana/kibana/"
  service:
    name: kibana
